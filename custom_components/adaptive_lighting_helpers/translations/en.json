{
  "config": {
    "step": {
      "user": {
        "title": "Adaptive Lighting Helpers",
        "description": "Registers the adaptive_lighting_helpers.compute_lighting_groups, compute_curve, compute_scene_coverage, and apply_lighting services. Nothing else to configure here - add day-phase/curve sensors afterwards from this integration's own page (Add Sensor)."
      }
    },
    "abort": {
      "already_configured": "Already configured"
    }
  },
  "config_subentries": {
    "sensor": {
      "entry_type": "Sensor",
      "initiate_flow": {
        "user": "Add Sensor"
      },
      "step": {
        "user": {
          "title": "Add an Adaptive Lighting sensor",
          "description": "Adds a day-phase/curve sensor and phase-override select, grouped under a device with this name. Entities are prefixed with it too (e.g. \"Living Room\" -> sensor.living_room_adaptive_lighting). The schedule times and brightness/colour curve aren't set here - they're separate entities on the device once it's created, each starting at a sensible default and adjustable any time.",
          "data": {
            "name": "Name"
          }
        }
      },
      "error": {
        "already_configured": "A sensor with this name already exists"
      }
    }
  },
  "issues": {
    "missing_two_step_label": {
      "title": "Bulbs missing the two-step transition label",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Apply the two-step transition label",
            "description": "{count} light(s) look like bulbs that can't transition brightness and colour temperature in one command, but aren't labelled, so Adaptive Lighting Helpers is sending them a single combined command - which those bulbs handle badly.\n\n{lights}\n\nSubmitting applies the \"{label}\" label to them (on the device, so it survives entity renames), creating the label if it doesn't exist yet. Nothing else changes. If you'd rather leave them as they are, ignore this repair instead."
          }
        },
        "abort": {
          "entry_gone": "Adaptive Lighting Helpers was removed, so there's nothing to label."
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Adaptive Lighting Helpers options",
        "description": "Bulb models that need two-step transitions, one pattern per line. This is the complete list the check uses — the box starts pre-filled with the models shipped with the integration, and you can add to it or remove from it freely.\n\nPatterns are matched case-insensitively against “manufacturer model”, and * is a wildcard, e.g. *TRADFRI bulb* or IKEA*. Clearing the box entirely falls back to the shipped defaults; to stop being told about unlabelled bulbs, ignore the repair instead.\n\nOnce you save your own list it stays as you left it — a later release adding a newly discovered bulb won't change it.\n\nPer-owner sensors (optional): for each automation that writes lights through this integration, adds two counters - how many of its lights it is currently driving, and how many are currently held by something else. Both are plain numbers you can put on any dashboard or build automations on, without the custom write-tracking card. A light being held elsewhere is a supported outcome, not a fault. Off by default because a busy house gains a couple of dozen entities; turning it off again removes them.",
        "data": {
          "two_step_models": "Two-step bulb models",
          "owner_sensors": "Create a sensor pair per owner"
        }
      }
    }
  }
}
